<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="Grocery.App.Views.GroceryListItemsView"
    x:Name="RootPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="{Binding Title}">

    <ScrollView>
        <VerticalStackLayout Padding="12" Spacing="12">

            <!-- Eerste CollectionView: huidige items op de boodschappenlijst -->
            <Label Text="Inhoud boodschappenlijst" FontAttributes="Bold" />
            <CollectionView ItemsSource="{Binding MyGroceryListItems}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Grid Padding="8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <Label Text="{Binding Product.Name}" Grid.Column="0" VerticalOptions="Center" />
                            <Label Text="{Binding Amount}" Grid.Column="1" VerticalOptions="Center" />
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <!-- SearchBar voor beschikbare producten -->
            <SearchBar
                Placeholder="Zoek producten"
                Text="{Binding SearchText, Mode=TwoWay}"
                SearchCommand="{Binding SearchCommand}"
                SearchCommandParameter="{Binding SearchText}"
                SearchButtonPressed="OnSearchButtonPressed" />

            <Label Text="Beschikbare producten" FontAttributes="Bold" />
            <!-- Tweede CollectionView: beschikbare (gefilterde) producten -->
            <CollectionView ItemsSource="{Binding FilteredProducts}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Grid Padding="8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <Label Text="{Binding Name}" Grid.Column="0" VerticalOptions="Center" />
                            <Button Text="Toevoegen"
                                    Grid.Column="1"
                                    Command="{Binding Source={x:Reference RootPage}, Path=BindingContext.AddProductCommand}"
                                    CommandParameter="{Binding .}" />
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>